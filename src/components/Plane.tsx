import * as React from 'react';
import * as d3 from 'd3';

export interface PlaneProps {}
export interface PlaneState {}

export class Plane extends React.Component<PlaneProps, PlaneState> {
    constructor(props: PlaneProps) {
        super(props);
    }
}